import React from "react";

const homePagePath = "/";
const registerPath = "/register";
const loginPath = "/login";
const newPasswordPath = "/reset-password";

export { registerPath, homePagePath, loginPath, newPasswordPath };
